# VERIFY_PLAN.md — Excel VBA Assistant 人工端到端验证手册

> 来源：`.trae/documents/mcp-managed-excel-refactor.md` T9 章节（:528-546）记录的 11 项"无法自动验证"项。
> 用途：上一任 AI 只完成了构建与打包（自动验证通过），所有真实环境行为都没人验证过。本手册把那 11 项补全成可照做的步骤。
> 步骤依据：实际代码 `src/extension.ts` / `src/status/statusBar.ts` / `src/output/outputChannel.ts` / `src/native/processUtils.ts` / `src/mcp/serverManager.ts` 与 README，所有"预期结果"里的界面文字都是从代码里抄的原文，照着对即可。
> 制作日期：2026-09-13。本手册为唯一新增文件，未改动任何代码。

---

## 一、环境准备清单（开始前逐项打勾）

| # | 准备项 | 要求 | 说明 |
|---|---|---|---|
| 1 | 操作系统 | Windows 10 / 11 | 非 Windows 插件直接警告不工作（extension.ts:72-74） |
| 2 | Excel | 桌面版 2016 及以上 | 必须是本机安装的桌面版，不能用网页版 |
| 3 | 编辑器 | Trae 或 VS Code，版本 ≥ 1.85 | 二选一即可 |
| 4 | Excel 信任设置（**必做，最常见失败原因**） | 打开"信任对 VBA 项目对象模型的访问" | 路径：Excel → 文件 → 选项 → 信任中心 → 信任中心设置 → 宏设置 → 勾选**「信任对 VBA 项目对象模型的访问」** → 确定。不开的话所有 VBA 读写都会报错 |
| 5 | 安装包 | `D:\excel-vba-assistant\excel-vba-assistant-0.8.6.vsix`（约 66MB） | 已内嵌 mcp-excel.exe，安装和运行都**不需要联网** |
| 6 | 测试目录 | 新建 `D:\vbatest`，并在编辑器中**打开该文件夹作为工作区** | 插件激活时要往工作区写 `.trae\mcp.json`，没有打开的工作区这步会静默跳过 |
| 7 | 测试工作簿 | 在 `D:\vbatest` 里准备一个 `demo.xlsm` | 做法：Excel 新建工作簿 → 按 Alt+F11 打开 VBA 编辑器 → 菜单"插入 → 模块" → 粘贴下方测试宏 → 回到 Excel，另存为 → 保存类型选"Excel 启用宏的工作簿(*.xlsm)" |
| 8 | 干净进程环境 | 关闭其他所有 Excel 窗口 | 插件靠 PID 区分 Excel 实例，多开容易混淆 |
| 9 | 任务管理器 | 会用到：Ctrl+Shift+Esc → "详细信息"标签 | 验证 Excel 进程是否退出/残留 |

测试宏（粘贴到模块里）：

```vba
Sub TestMain()
    Worksheets("Sheet1").Range("A1").Value = "sync-test"
End Sub
```

安装插件（两种方式**二选一**，不要同时装两个实例，否则两个实例会抢着管理同一个 Excel）：

- **方式 A（推荐，最简单）**：编辑器按 Ctrl+Shift+P → 输入并执行 **`Extensions: Install from VSIX...`**（中文界面：`扩展: 从 VSIX 安装...`）→ 选择第 5 项的 .vsix 文件 → 完成后按提示重载窗口。
- **方式 B（开发模式）**：用编辑器打开 `D:\excel-vba-assistant` 项目文件夹 → 按 F5 → 会先自动构建，然后弹出一个新的"扩展开发宿主"窗口，在那个窗口里做后续所有验证。（注意：如果你机器上之前装过 0.8.6 的正式版，先卸载它再 F5。）

**验证过程中会反复用到的三个界面位置**：

1. **输出日志面板**：Ctrl+Shift+U（或 菜单 查看 → 输出），右上角下拉框选择 **`Excel VBA Assistant`** 通道。日志行格式：`[时:分:秒] [INFO/WARN/ERROR] 内容`。
2. **控制面板**：左侧活动栏的 **Excel VBA** 图标，面板里有"选择文件 / 断开 Excel / 选择目录 / 同步至目录 / 写回Excel / 自动同步 / Excel 置顶"等按钮。
3. **状态栏**：编辑器**底部左侧**，一项叫"VBE 同步"，旁边有小图标。**点它可以跳到输出日志**。

---

## 二、11 项验证

### V1 扩展宿主启动（T9 第 1 项）

- **目的**：插件能在你的编辑器里正常激活、UI 全部出现。
- **前置条件**：环境准备清单 1-7 全部完成，编辑器已打开 `D:\vbatest` 工作区。
- **操作步骤**：
  1. 按方式 A 或 B 安装插件，重载窗口（Ctrl+Shift+P → `Developer: Reload Window` / `开发人员: 重新加载窗口`）。
  2. 看左侧活动栏是否出现 **Excel VBA** 图标，点一下。
  3. Ctrl+Shift+P 输入 `Excel VBA`，看命令列表。
- **预期结果**：活动栏出现 Excel VBA 图标；点击后打开控制面板（含"路径 / 同步操作 / 配置选项"三个区域）；命令面板能搜到 `Excel VBA: 选择 Excel 文件`、`Excel VBA: VBE → 本地 同步`、`Excel VBA: 打开输出日志` 等命令；底部状态栏出现"VBE 同步"字样（空心圆图标）。
- **失败时记录什么**：安装环节的报错弹窗截图；命令搜不到的话记下能搜到哪些命令；打开输出面板选 `Excel VBA Assistant` 通道，把激活前后的日志截图（尤其 `[ERROR]` 行）；如果出现"激活失败"弹窗，抄下全文。

### V2 插件激活后 MCP server 启动成功（T9 第 2 项）

- **目的**：插件内嵌的 `mcp-excel.exe` 子进程被拉起并与插件连通（这是所有 Excel 操作的地基）。
- **前置条件**：V1 通过。
- **操作步骤**：
  1. Ctrl+Shift+P → 执行 `Developer: Reload Window` 重新加载，让插件从头激活一次。
  2. 打开输出面板（Ctrl+Shift+U），下拉框选 `Excel VBA Assistant`，从顶部往下看日志。
  3. 在资源管理器（左侧文件树）里查看工作区根目录是否生成了 `.trae` 文件夹，点开里面的 `mcp.json`。
- **预期结果**：日志里**依次**出现 `[INFO] Excel VBA Assistant 已激活` 和 `[INFO] mcp-server-excel 已启动`；`.trae\mcp.json` 存在，内容含 `"excel-mcp"` 和一个指向 `...\dist\mcp-excel.exe` 的路径；状态栏"VBE 同步"前是**空心圆**图标。
- **失败时记录什么**：若出现 `[ERROR] 启动 mcp-server-excel 失败：...`，抄下冒号后面的完整原因；状态栏是否变成红色 `(error) VBE 同步`；`.trae\mcp.json` 是否没生成；杀毒软件是否弹过拦截提示（截图）；任务管理器里有没有 `mcp-excel.exe` 进程。

### V3 选择文件后 Excel 自动打开（T9 第 3 项）

- **目的**：验证新架构的核心承诺——插件是 Excel 的启动入口，选完文件 Excel 自己开。
- **前置条件**：V2 通过；`demo.xlsm` **没有**被任何 Excel 打开；机器上没有其他 Excel 进程。
- **操作步骤**：
  1. 控制面板点 **「选择文件」**（或 Ctrl+Shift+P → `Excel VBA: 选择 Excel 文件`）。
  2. 在弹出的文件框里选 `D:\vbatest\demo.xlsm`。
  3. 不要做任何操作，等 5-10 秒，观察桌面。
- **预期结果**：Excel 自动启动并显示 demo.xlsm 窗口；日志出现 `[INFO] 已选择 Excel 文件：D:\vbatest\demo.xlsm` → `[INFO] Excel 已通过 MCP 打开并连接` → `[INFO] 已记录 Excel 进程 PID：xxxx`。
- **失败时记录什么**：错误弹窗全文（形如"无法打开 Excel 文件：..."）；Excel 到底开没开；**特别核对第三条 PID 日志**——如果出现的是 `[WARN] 未能探测到 MCP 创建的 Excel 进程 PID`，本项算"部分通过"，但要记录下来（它会让 V5 的进程清理变弱，见第四节第 2 条）；打开状态栏 tooltip（鼠标悬停在"VBE 同步"上）截图，看"Excel PID"和"MCP 会话"两行。

### V4 状态栏显示"已连接 / VBE 同步 · MCP"（T9 第 4 项）

- **目的**：验证状态指示正确反映"MCP 托管"模式。
- **前置条件**：V3 刚完成，Excel 处于打开状态。
- **操作步骤**：
  1. 看编辑器底部状态栏"VBE 同步"项的图标和文字。
  2. 鼠标悬停在上面，等 tooltip 展开，逐行核对。
- **预期结果**：状态栏显示 **实心圆 + `VBE 同步 · MCP`**（绿色，代码里的 connected 态，statusBar.ts:42）；tooltip 包含：`Excel 文件：D:\vbatest\demo.xlsm`、`Excel PID：一个具体数字`、`MCP 会话：活跃`、`服务状态：已连接（未同步）`。
- **失败时记录什么**：tooltip 整体截图；如果文字里**没有"· MCP"后缀**或"MCP 会话"显示"未激活"，说明 V3 的会话标记没写进去，把 V3 到 V4 之间的日志一起截图；如果显示红色 `(error) VBE 同步`，抄 tooltip 里"最近错误"一行。

### V5 点「断开 Excel」后状态回到 disconnected（T9 第 5 项）

- **目的**：验证清理链路——关会话、保存、进程退出、状态复位，一条龙。
- **前置条件**：V3/V4 通过（Excel 正被插件托管）；**先在 Excel 里按 Ctrl+S 保存**（正常应自动保存，这里是保险）。
- **操作步骤**：
  1. 控制面板点 **「断开 Excel」**。
     ⚠️ 注意：**命令面板里是搜不到"断开"命令的**（该命令没在 package.json 注册，属已知缺陷），必须用面板按钮。
  2. 观察 Excel 窗口和状态栏。
  3. 打开任务管理器 → 详细信息，搜 `EXCEL`。
- **预期结果**：Excel 窗口自动关闭；编辑器右下角弹通知 **"已断开与 Excel 的连接"**；状态栏回到空心圆"VBE 同步"；tooltip 显示 `服务状态：未连接`、`Excel PID：未记录`、`MCP 会话：未激活`；任务管理器**没有**残留的 EXCEL.EXE。日志顺序应为：`[INFO] 用户手动断开 Excel 连接，开始重置服务状态` → `MCP session 已关闭并保存工作簿` → `等待 Excel 进程 xxxx 退出（最多 5 秒）` → `Excel 进程已正常退出` → `服务状态已重置`。
- **失败时记录什么**：日志若出现 `[WARN] Excel 进程 xxxx 未在 5 秒内退出，执行强制终止`——记录下来并截图 Excel 关闭时的表现（是否闪退/弹恢复对话框）；任务管理器若有残留 EXCEL.EXE，记下它的 PID；任何错误弹窗全文；状态栏 tooltip 截图（确认 PID/会话是否真的清空）。

### V6 手动执行「VBE → 本地」同步成功（T9 第 6 项）

- **目的**：验证核心功能——把 Excel 里的 VBA 代码导出成本地文件（AI 改代码的前提）。
- **前置条件**：V3 通过（Excel 托管中）；还没设置过同步目录。
- **操作步骤**：
  1. 控制面板点 **「选择目录」**，新建并选择 `D:\vbatest\sync` 作为同步目录。
  2. Ctrl+Shift+P → 执行 **`Excel VBA: VBE → 本地 同步`**（或面板点「同步至目录」）。
  3. 打开 `D:\vbatest\sync` 看生成了什么；同时看状态栏和日志。
- **预期结果**：`D:\vbatest\sync` 下出现 `workbook.json` 和 `模块\`（里面有你粘贴的测试模块，如 `模块1.bas`）、`类模块\`、`窗体\`、`Microsoft Excel 对象\` 等文件夹；打开 `模块1.bas` 能看到 TestMain 宏代码；状态栏短暂出现 **✓（已同步）**；日志出现"同步"相关的 `[INFO]` 行。
- **失败时记录什么**：报错"无法访问 VBAProject"→ 九成是环境准备第 4 项的信任设置没开（回去勾选重试，不算代码缺陷但要在表里注明）；目录没生成或为空 → workbook.json 内容截图 + 日志全文；状态栏是否变红。

### V7 手动执行「本地 → VBE」同步成功（T9 第 7 项）

- **目的**：反向验证——本地改的代码能覆盖写回 Excel 的 VBE。
- **前置条件**：V6 通过；Excel 托管中。
- **操作步骤**：
  1. 用记事本打开 `D:\vbatest\sync\模块\模块1.bas`。
  2. 在 `Sub TestMain()` 和 `End Sub` 之间加一行 `Debug.Print "edited-locally"`，保存关闭。
  3. 控制面板点 **「写回Excel」**（或 Ctrl+Shift+P → `Excel VBA: 本地 → VBE 同步`）。
  4. **此时必须弹一个确认对话框**——点确认。
  5. 回到 Excel，按 Alt+F11 打开 VBA 编辑器，查看模块代码。
- **预期结果**：第 4 步出现确认弹窗（提示会覆盖 VBE 代码）；确认后 VBE 编辑器里的代码包含刚加的那一行；状态栏短暂 ✓；日志出现"同步完成"类 INFO 行。
- **失败时记录什么**：**没弹确认框直接执行**（这本身是要记录的异常）；VBE 里代码没变；报错全文（"文件正被占用"类错误 → 检查是不是记事本还没关）；截图 VBE 代码窗口。

### V8 开启自动同步后，本地修改自动同步到 VBE（T9 第 8 项）

- **目的**：验证无人值守的双向同步（AI 干活的日常形态）。
- **前置条件**：V7 通过。
- **操作步骤**：
  1. 控制面板把 **「自动同步」** 开关打开。
  2. 用记事本再改一次 `模块1.bas`（比如把那行改成 `Debug.Print "auto-sync-test"`）→ **Ctrl+S 保存** → 什么都不按，等 1-3 秒。
  3. Alt+F11 看 VBE 代码有没有自己变。
  4. 反向测：在 VBE 里直接改代码（比如加一行注释 `' from-vbe`），不要保存之外的任何操作，等 3-6 秒（VBE 轮询间隔是 3 秒），再用记事本看本地 `.bas` 文件有没有自己变。
- **预期结果**：第 2→3 步，VBE 代码自动出现 `auto-sync-test`；第 4 步，本地文件自动出现 `' from-vbe`；期间状态栏短暂转圈/✓；输出面板出现自动同步日志。
- **失败时记录什么**：**记录失败方向**（本地→VBE 失败，还是 VBE→本地失败）；本地→VBE 失败先自查：文件真的保存了吗？（磁盘上没保存就不会触发——这是设计，编辑器建议开自动保存）；VBE→本地失败注意等满 6 秒；输出面板日志全文截图；**如果日志出现两边来回刷同步记录（反复覆盖）**，务必截图，那是最危险的 bug 形态。

### V9 AI 调用 MCP `range set-values` 成功（T9 第 9 项）

- **目的**：验证 AI 桥接——Trae 的 AI 能通过 MCP 直接操作插件托管中的 Excel。
- **前置条件**：V3 通过（Excel 托管中）；V2 生成的 `.trae\mcp.json` 存在；在 Trae 的 MCP 设置面板里确认出现了 **excel-mcp**（列表里显示 `excel-mcp (工作区)` 即正确；没有的话先重载窗口再确认一次）。
- **操作步骤**：
  1. 在 Trae 的 AI 对话框输入明确指令：**"调用 range 工具，把 demo.xlsm 的 Sheet1 的 A1:B2 设置为 1、2、3、4"**。
  2. 如果 AI 说找不到文件或会话，让它先执行 `file(open, "D:\vbatest\demo.xlsm")` 再重试。
  3. 看 Excel 窗口里的 Sheet1。
- **预期结果**：AI 回复调用成功；Excel 的 Sheet1!A1:B2 单元格显示 1、2、3、4。
- **失败时记录什么**：⚠️ **本项是全手册最重要的观察点**。插件自己启动了一个 mcp-excel.exe，Trae 又会按 `.trae\mcp.json` 启动**另一个**独立的 mcp-excel.exe——两个进程的会话大概率不互通，AI 那边可能报"文件已被另一个实例打开/无法打开"。**把 AI 回复的完整原文截图**，尤其报错原文；记录 Excel 单元格有没有被写入；这项失败不代表同步功能坏（V6-V8 已单独验证过），如实记录即可，它是架构决策的关键输入。

### V10 AI 调用 MCP `table create` 成功（T9 第 10 项）

- **目的**：验证 AI 能调用第二类工具（结构类操作），确认 V9 不是巧合。
- **前置条件**：V9 通过，或至少 Excel 仍被托管且 AI 能调用工具。
- **操作步骤**：在 AI 对话框输入：**"在 demo.xlsm 的 Sheet1 上，用 A1:B2 区域的数据创建名为 Sales 的表格"**。
- **预期结果**：AI 回复创建成功；Excel 里 A1:B2 变成带筛选按钮和表样式的表格，表名 Sales。
- **失败时记录什么**：AI 回复全文截图；报错里工具名是什么写法（文档约定工具名不带 `excel_` 前缀，如果报"找不到工具"记下它实际尝试的名字）；Excel 里的实际状态。

### V11 用户先打开文件时，插件提示并允许接管（T9 第 11 项）

- **目的**：验证冲突处理——用户手滑先开了文件的场景，插件要给出清晰出路而不是直接报错。
- **前置条件**：V5 已断开（插件空闲）；`demo.xlsm` 已保存；**这是唯一会"杀"你自己 Excel 的测试，确认没有其他重要 Excel 窗口开着**。
- **操作步骤**：
  1. 在文件管理器里**双击** `demo.xlsm`，用 Excel 正常打开它。
  2. 回到编辑器，控制面板点「选择文件」，选**同一个** demo.xlsm。
  3. 应弹出模态提示 → **先点「取消」**，确认安全路径。
  4. 再次「选择文件」选同一文件 → 这次点 **「关闭并重新打开」**。
  5. 观察 Excel 的关闭与重开，再看状态栏。
- **预期结果**：第 2 步弹出提示：**"检测到 demo.xlsm 已在另一个 Excel 实例中打开。请关闭后重试，或让插件接管（关闭当前 Excel 并由插件重新打开）。"**，带「关闭并重新打开」「取消」两个按钮；点取消后状态不变，日志出现 `用户取消接管已打开的 Excel 文件`；点接管后：旧 Excel 保存并关闭 → 新 Excel 自动打开 → 状态栏回到"VBE 同步 · MCP"实心圆；日志出现 `用户选择接管由 PID xxxx 打开的 Excel 实例`、`等待 Excel 进程 xxxx 退出（最多 5 秒）`。
- **失败时记录什么**：没弹提示、直接报错 → 报错全文；接管后 Excel 没有重开 → 日志全文；任务管理器里是否出现**多个** EXCEL.EXE 残留（记 PID）；Excel 是否弹过"文件损坏/恢复"对话框（截图，说明强杀伤到了文件）。

---

## 三、最容易出问题的地方（基于 v0.5.6-v0.8.4 的翻车史 + 代码现状）

那段历史：关闭检测被重写了约 19 次（v0.5.6→v0.8.4），v0.8.0 换窗口标题检测、v0.8.2 整体回退，最后整个功能被放弃。所以：

1. **插件不会检测 Excel 关闭——这是设计，不是 bug。** 手动关掉 Excel 后状态栏会继续显示"已连接"，别误判。所有清理必须走「断开 Excel」按钮。验证 V3-V8 期间**不要手动关 Excel**，否则后面的项全部失真。
2. **PID 探测会静默失败。** V3 若只出现 `[WARN] 未能探测到 MCP 创建的 Excel 进程 PID`，插件不阻断、状态照样"已连接"（extension.ts:404 设 0），但 V5 断开时的"等待退出+强杀兜底"会整个跳过——**Excel 残留的概率大增**。V3 时务必核对 PID 日志；每项测完顺手看一眼任务管理器。
3. **别最小化 Excel。** 最小化窗口在历史上引发过三连修的误判（v0.6.5 / v0.7.4 / v0.7.5），验证时保持 Excel 窗口正常显示。
4. **状态栏 ✓（已同步）5 秒后会自动回落成实心圆**——是故意设计的（statusBar.ts:7），不是状态丢失。
5. **「写回Excel」有确认弹窗，「自动同步」是静默的。** 自动同步开着时，本地改动会不打招呼地覆盖 VBE——测试用的 xlsm 别放真数据。
6. **接管和断开都可能强杀进程树**（terminateProcessTree），未保存的内容会直接丢。
7. **vsix 安装版和 F5 开发版二选一**，两个实例会互相抢 Excel。
8. **V9/V10 的双进程疑点**（插件一个 exe、Trae 一个 exe）：失败时如实记录，它不影响 V6-V8 的同步结论，但直接决定架构要不要改。

---

## 四、结果记录表

测完一项填一行（通过=✅ / 失败=❌ / 部分通过=⚠️）：

| 编号 | 通过 | 失败 | 失败现象 | 备注 |
|---|---|---|---|---|
| V1 扩展宿主启动 | ☐ | ☐ | | |
| V2 MCP server 启动 | ☐ | ☐ | | |
| V3 选择文件自动打开 Excel | ☐ | ☐ | | 若 PID 探测 WARN 务必备注 |
| V4 状态栏 已连接/·MCP | ☐ | ☐ | | 附 tooltip 截图 |
| V5 断开 Excel 清理 | ☐ | ☐ | | 任务管理器有无残留 EXCEL.EXE |
| V6 VBE→本地 同步 | ☐ | ☐ | | 信任设置未开不算代码缺陷 |
| V7 本地→VBE 同步 | ☐ | ☐ | | 有无确认弹窗 |
| V8 自动同步（双向） | ☐ | ☐ | | 记录失败方向；有无反复覆盖 |
| V9 AI range set-values | ☐ | ☐ | | AI 报错原文最重要 |
| V10 AI table create | ☐ | ☐ | | |
| V11 冲突提示与接管 | ☐ | ☐ | | 取消/接管两条路径分别记录 |

---

*手册完。全部为只读整理，未改动任何代码。验证完成后，建议把结果表和异常日志回填到 `.trae/documents/mcp-managed-excel-refactor.md` 的进度日志章节（那是上一任定下的规矩）。*
