# EXPERIMENT_ATTACH.md — "一行实验"操作手册：裁定 MCP 能否连接已打开的 Excel

> 目的：FEASIBILITY.md 第三节指出一个证据冲突——
> - `tools/schemas.json:1454`（能力快照）说：文件已打开时"复用现有会话"；
> - `.trae/documents/mcp-managed-excel-refactor.md:191`（07-01 实测记录）说：`file(open)` 报错 **"Excel files must not be open in another Excel instance"**。
>
> 这个冲突决定可视化路线怎么走：**支持 attach → 直接通；不支持 → 走 COM 桥**。本手册用三个最小子实验把它裁定清楚。全程不需要写代码。
>
> 制作日期：2026-09-14。本文件为唯一新增产物，未改动任何代码。

---

## 一、环境准备清单

| # | 准备项 | 怎么做 | 怎么确认就绪 |
|---|---|---|---|
| 1 | 操作系统 + Excel | Windows 10/11，装有桌面版 Excel 2016+ | 能正常双击打开一个 Excel 文件 |
| 2 | 编辑器 | 安装了 Trae（VS Code 亦可，下文统称"编辑器"） | 能打开文件夹、有 AI 对话框 |
| 3 | 插件 | 编辑器中 Ctrl+Shift+P → `Extensions: Install from VSIX...`（`扩展: 从 VSIX 安装...`）→ 选择 `D:\excel-vba-assistant\excel-vba-assistant-0.8.6.vsix` | 重载窗口后左侧活动栏出现 **Excel VBA** 图标 |
| 4 | 测试目录与文件 | 新建文件夹 `D:\attach-test`；在 Excel 里新建一个空白工作簿，**另存为** `D:\attach-test\demo.xlsm`（保存类型选"Excel 启用宏的工作簿"）——文件里不需要任何宏，但必须是 .xlsm，因为插件的选择文件对话框只认 xlsm/xlsb/xlam/xls | 资源管理器里能看到 `D:\attach-test\demo.xlsm` |
| 5 | 测试工作区 | 在编辑器中 **打开文件夹** `D:\attach-test`（这是关键：插件激活时会往工作区写 `.trae\mcp.json`，AI 的 MCP 全靠它） | 文件树里出现 `.trae` 文件夹，里面有 `mcp.json`，内容含 `"excel-mcp"` 字样；没有的话重载一次窗口（Ctrl+Shift+P → 重新加载窗口）再等几秒 |
| 6 | Trae 的 MCP 面板 | 打开 Trae 设置 → MCP | 列表出现 **excel-mcp**（带"工作区"标记即可）。没有就重载窗口再等几秒 |
| 7 | 任务管理器 | Ctrl+Shift+Esc → "详细信息"标签 → 点"名称"列排序 | 后面每个子实验都要用它**数 EXCEL.EXE 的个数**，这是裁定关键证据 |
| 8 | 干净起点 | 关闭所有 Excel 窗口和文件管理器里打开的预览 | 任务管理器里搜 `EXCEL`，结果为 0 个 |

> 本实验**不需要**开启"信任对 VBA 项目对象模型的访问"（不碰 VBA，只碰文件和单元格）。
> 本实验**不需要**联网（mcp-excel.exe 已内嵌在插件里）。

**三个通用观察手段（后面反复用到）**：

1. **数 Excel 进程**：任务管理器 → 详细信息 → 名称列找 `EXCEL.EXE`。有几个就是几个实例。
2. **插件日志**：编辑器底部状态栏点"VBE 同步"字样（或 Ctrl+Shift+U 选 `Excel VBA Assistant` 通道）。插件打开文件成功会打 `[INFO] Excel 已通过 MCP 打开并连接`。
3. **AI 的原话**：Trae AI 对话框里，凡是要它调工具，都加一句 **"把工具返回的原始结果完整贴给我，成功或报错都不要省略"**——报错原文是本实验的裁定依据。

---

## 二、实验步骤

> 三个子实验按 A → B → C 顺序做，**每个之间做一次"清理复位"**（见每项末尾）。
> 如果 AI 执行工具前先反问你"要不要打开文件"之类，回复"直接执行，把原始结果贴给我"。

### 子实验 A：用户先双击打开文件，AI 再 `file(open)` 同一文件（裁定核心冲突）

- **目的**：直接裁定 schemas.json（"复用"）与工程文档（"拒绝"）谁对。这是分岔点。
- **前置条件**：环境就绪；任务管理器中 EXCEL.EXE 数量为 **0**。
- **操作步骤**：
  1. 在文件管理器里**双击** `D:\attach-test\demo.xlsm`，等 Excel 正常显示窗口。
  2. 任务管理器确认：EXCEL.EXE 现在是 **1 个**，记住这个数。
  3. 切到 Trae 的 AI 对话框，原样发送：
     > 使用 file 工具的 open 动作打开这个文件：D:\attach-test\demo.xlsm 。把工具返回的原始结果完整贴给我，成功或报错都不要省略。
  4. 等 AI 执行完（file open 默认超时 120 秒，最长等两分钟）。
  5. 不管成功失败，再发一句：
     > 使用 file 工具的 list 动作列出当前所有会话，原始结果贴给我。
  6. 再发一句：
     > 使用 range 工具的 get-values 动作读取 demo.xlsm 的 Sheet1!A1:B2，原始结果贴给我。
  7. 看一眼任务管理器：EXCEL.EXE 现在几个？
  8. 看一眼 Excel 窗口：A1:B2 有没有被写入任何东西。
- **预期现象（两种可能）**：
  - **可能 1（复用/attach 成功）**：第 3 步 AI 返回了 `session_id`（一串编号），没有报错；第 6 步能读出单元格内容（空的话返回空表也算成功）；EXCEL.EXE 仍是 **1 个**。
  - **可能 2（拒绝）**：第 3 步 AI 返回报错，原文类似 **"Excel files must not be open in another Excel instance"**；第 6 步因此失败。
  - **可能 3（假复用，最需要警惕）**：第 3 步返回了 session_id，但任务管理器变成 **2 个** EXCEL.EXE，或第 8 步的写入出现在了一个"自己冒出来的"新 Excel 窗口里——说明 server 嘴上说复用、实际又开了一个隐藏实例。
- **要记录什么**：第 3 步 AI 回复**完整原文**（截图 + 复制文字，报错一句话都不能漏）；第 5、6 步的原始返回；第 7 步 EXCEL.EXE 个数（前后对比）；第 8 步窗口现象。
- **清理复位**：如果 Excel 还开着，直接关掉（弹保存提示选"不保存"）；回任务管理器确认 EXCEL.EXE 归零。

### 子实验 B：用户先双击打开文件，再让**插件**选同一文件（验证插件的冲突接管）

- **目的**：验证插件自己的冲突检测与"接管"流程是否可靠（extension.ts:332-380 实现了弹窗接管，但从未被实测过）。它与 A 独立成证：即使 MCP 拒绝，插件接管成功也能兜底出一条用户体验路径。
- **前置条件**：清理复位完成（EXCEL.EXE = 0，插件状态栏"VBE 同步"是空心圆）。
- **操作步骤**：
  1. 双击打开 `demo.xlsm`，确认 Excel 窗口显示。
  2. 切到编辑器，点左侧活动栏 **Excel VBA** 图标打开控制面板。
  3. 点 **「选择文件」**，选同一个 `D:\attach-test\demo.xlsm`。
  4. 观察是否弹出模态提示，按提示操作：应出现 **"检测到 demo.xlsm 已在另一个 Excel 实例中打开。请关闭后重试，或让插件接管（关闭当前 Excel 并由插件重新打开）。"**，按钮是「关闭并重新打开」和「取消」。
  5. **先点「取消」**，确认安全路径（状态不变、Excel 没被关）。
  6. 再点一次「选择文件」选同一文件，这次点 **「关闭并重新打开」**。
  7. 观察旧 Excel 是否被关闭、新 Excel 是否自动打开；打开任务管理器数 EXCEL.EXE。
  8. 看插件日志（`Excel VBA Assistant` 通道），找 `用户选择接管由 PID xxxx 打开的 Excel 实例`、`等待 Excel 进程 xxxx 退出`、`Excel 已通过 MCP 打开并连接` 这几行。
- **预期现象**：
  - 顺利路径：第 4 步弹窗出现 → 第 5 步取消无副作用 → 第 6-7 步旧实例被保存关闭、新实例由插件打开，EXCEL.EXE 稳定在 **1 个**，状态栏变绿色"VBE 同步 · MCP"。
  - 异常路径 1：**没有弹窗**，直接报错（说明插件的冲突检测没发现用户实例，WMI 探测失效）——记录报错原文，这本身是有效结论。
  - 异常路径 2：弹窗出现、点了接管，但 Excel 关了没重开，或出现 **2 个** EXCEL.EXE 残留——记录日志全文（尤其 `未在 5 秒内退出，执行强制终止`）。
- **要记录什么**：弹窗截图（有无、完整文案）；每次操作后 EXCEL.EXE 个数；`Excel VBA Assistant` 通道从第 3 步到第 8 步的完整日志截图；状态栏最终样子。
- **清理复位**：控制面板点 **「断开 Excel」**（注意：命令面板里搜不到"断开"命令，必须用面板按钮）；等 Excel 关闭，任务管理器确认 EXCEL.EXE 归零；确认状态栏回到空心圆。

### 子实验 C：插件打开文件（show:true）后，确认窗口前台可见、AI 操作落在同一个可见实例上

- **目的**：验证"插件开、插件管"路线的两个承诺——①插件打开的 Excel 用户看得见（`show:true`，sessionStore.ts:31）；②AI 经 Trae 操作时作用在**同一个可见窗口**上，而不是另一个隐藏实例。
- **前置条件**：清理复位完成；**这次不要手动打开任何 Excel**。
- **操作步骤**：
  1. 编辑器里控制面板点 **「选择文件」** → 选 `D:\attach-test\demo.xlsm`。
  2. 观察：Excel 是否自动打开、窗口是否**前台可见**（不是只在任务栏里）；状态栏是否变"VBE 同步 · MCP"。日志应有 `[INFO] Excel 已通过 MCP 打开并连接`。
  3. 任务管理器：记下 EXCEL.EXE 个数（应为 **1 个**）。
  4. 切到 Trae AI 对话框，发送：
     > 使用 file 工具的 list 动作列出当前所有会话，原始结果贴给我。
  5. 再发送：
     > 使用 range 工具的 set-values 动作，把 demo.xlsm 的 Sheet1!A1 写成 42，把原始结果贴给我。
  6. **眼睛盯着 Excel 窗口**：A1 有没有变成 42？是哪个窗口变的？
  7. 任务管理器：EXCEL.EXE 现在几个？
- **预期现象**：
  - **理想结果**：第 1-2 步 Excel 自动打开且前台可见；第 4 步 list **可能返回空**（注意：插件的会话在插件自己的 mcp-excel.exe 进程里，Trae 的 AI 连的是**另一个** exe 进程，两边会话不互通——list 为空是预期内的，如实记录）；第 5 步 AI 要么报错（文件被占用/已在其他实例打开——同 A 的冲突），要么返回 session 并写入；**关键看第 6-7 步：写入出现在可见窗口里、EXCEL.EXE 始终 1 个 = 两个进程实际共享了同一个 Excel 实例（COM 层收敛），这是最好的结果**；如果冒出第二个 EXCEL.EXE 或写入落在别的窗口 = 会话隔离被证实。
  - 第 5 步如果报"文件已在其他实例打开"：记录原文——它把 A 的结论在"插件托管"场景下再验证一遍。
- **要记录什么**：第 2 步窗口可见性（截图：Excel 前台 + 编辑器状态栏同框）；第 4、5 步 AI 原文；第 6 步 A1 的值和所在窗口；第 7 步 EXCEL.EXE 个数（与第 3 步对比）；全程插件日志。
- **收尾**：控制面板「断开 Excel」→ 任务管理器确认归零。

---

## 三、结果记录表

| 编号 | 通过 | 失败 | 失败现象原文 | 备注 |
|---|---|---|---|---|
| A1 file(open) 对已打开文件 | ☐ | ☐ | | 通过=返回 session_id；失败=报错（原文必填）；"假复用"也记在这里 |
| A2 file(list) 会话列表 | ☐ | ☐ | | |
| A3 range 读值是否可用 | ☐ | ☐ | | |
| A4 EXCEL.EXE 个数（前→后） | ☐ | ☐ | | 例：1→2 |
| B1 冲突弹窗出现 | ☐ | ☐ | | 附弹窗截图 |
| B2 取消路径无副作用 | ☐ | ☐ | | |
| B3 接管后新实例打开 | ☐ | ☐ | | |
| B4 EXCEL.EXE 个数（前→后） | ☐ | ☐ | | |
| C1 插件打开且窗口前台可见 | ☐ | ☐ | | 附截图 |
| C2 AI file(list) 返回 | ☐ | ☐ | | 空=预期内，如实记录 |
| C3 AI 写入落在可见窗口 | ☐ | ☐ | | 记 A1 单元格最终值 |
| C4 EXCEL.EXE 个数（前→后） | ☐ | ☐ | | 1→1 是最好结果；1→2 证明会话隔离 |

---

## 四、结果解读指引（三种组合 → 三条路线）

拿记录表对照下面的组合，就能得出路线结论：

- **组合一：A1 返回 session_id，且 A4/C4 都是 1→1，C3 写入落在可见窗口**
  → **上游新版已支持 attach/复用，07-01 的报错记录已过时**。可视化走**路线甲：全 MCP 直连**——用户可以保持双击打开文件的习惯，AI 与插件都通过 MCP 接到同一个实例上，不需要 COM 桥，也不需要新组件。下一步只剩把这条行为写进文档和验收清单。
- **组合二：A1 报错"must not be open in another Excel instance"（B3 接管正常与否均可）**
  → **MCP 不支持连接用户已打开的实例**（07-01 记录成立）。可视化走**路线乙：COM 桥**——插件继续用已验证的按 PID attach（powershell.ts:130-195）操作用户实例，把 vbaClient.ts 里现成的受限操作（读单元格/建表等，:977-1278）包装暴露；MCP 只负责插件自己启动的实例。用户体验上用"插件开、插件管 + 弹窗接管"兜底（B 组实验就是验证这个兜底的）。
- **组合三：A1 返回了 session_id，但 A4/C4 出现 1→2，或 C3 的写入出现在另一个窗口/不可见实例**
  → **"假复用"：server 声称复用，实际另开了隐藏实例**。这是最坏也最有信息量的结果——意味着无论走哪条路线都必须先做**单实例收敛**（proxy 统一入口或强制复用插件实例），否则用户看到的工作簿和 AI 操作的工作簿不是同一个。请把截图和 EXCEL.EXE 个数完整带回来，先讨论再动手。
- **B 组单独说明**：即使 A 组完美，B3/B4 仍然重要——它验证的是"用户手滑先开文件"这个最常见场景的兜底体验；B1 若没弹窗（冲突检测失效），无论哪条路线都要先修 `findWorkbookOwnerPid`。

> 补充说明：本实验测的是 dist 里那颗 mcp-excel.exe（6-29 从上游 releases/latest 下载），上游新版本行为可能不同；如果结果落 in 组合二且你想确认"升级上游能不能直接解决"，那是后续联网核查的事项，不影响本次裁定。

*手册完。全程只读设计，未改动任何代码；执行实验本身也不需要改任何代码。*
